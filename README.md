# Loose Ends

An agent that handles the admin after someone dies. Built with Strands Agents for the AWS Agents for Humans hackathon.

Status: day 1 scaffold. See `docs/` for the spec.

## Develop

```
uv sync
uv run pytest
uv run loose-ends --help
```
